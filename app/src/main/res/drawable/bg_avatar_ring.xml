<?xml version="1.0" encoding="utf-8"?>
<!-- Presence ring behind member avatars; animated (scale + fade) when online. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent"/>
    <stroke android:width="2dp" android:color="@color/online_green"/>
</shape>
