<?xml version="1.0" encoding="utf-8"?>
<!-- Outer ring behind the PTT button. Scaled + tinted at runtime while
     transmitting (see WalkieTalkieFragment.animatePttRing). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent"/>
    <stroke android:width="2dp" android:color="#3300D4FF"/>
    <size android:width="96dp" android:height="96dp"/>
</shape>
